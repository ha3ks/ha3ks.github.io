---
layout: null
---

{
  "tags": {
    {% assign all_tags = site.tags %}
    {% for tag in all_tags %}
      "{{ tag[0] }}": {{ tag[1].size }}{% unless forloop.last %},{% endunless %}
    {% endfor %}
  }
}
